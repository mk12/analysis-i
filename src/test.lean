import standard
open eq.ops
open nat classical set
-- check or.swap
-- find_decl ((_ : Type) → _ → Type)
-- print prefix sigma
print notation


  -- Function application
  -- definition fun_apply {X Y : Set} (f : X => Y) {x : Object} (H : x ∈ X) :
  --     Object :=
  --   sigma.pr1 (f H)
  -- infix ` $ `:o60 := fun_apply
check @rfl
check default
